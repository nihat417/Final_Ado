<Window x:Class="WpfApp1.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="InfoWindow" Height="450" Width="800">
    <Grid>
        <TextBlock x:Name="txtbx1"
                   Margin="150"
                 Background="AliceBlue"
                 Foreground="Black"
                 FontSize="22"
                 Text="{Binding _selectedItem}">
        </TextBlock>

        <TextBlock x:Name="txtbx2"
                   Text="{Binding _selectedItem2}"
                   Width="195"
                   Height="40" Margin="209,316,516,78">
            
        </TextBlock>
        
    </Grid>
</Window>
